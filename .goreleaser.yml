before:
  hooks:
    - go mod tidy

builds:
  - id: payment-gateway
    main: ./cmd/api
    binary: payment-gateway
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - format: tar.gz
